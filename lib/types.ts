export type Role = 'owner' | 'admin' | 'member';
export type Workspace = { id: string; name: string; role: Role };
export type WhatsAppNumber = { id: string; workspace_id: string; label: string; instance_name: string; created_at: string; state?: string; phone?: string; error?: string };
export type Member = { user_id: string; email: string; role: Role; joined_at: string };
export type Contact = { id: string; phone: string; name: string };
export type Broadcast = { id: string; name: string; message: string; status: 'sending' | 'completed' | 'partial' | 'failed'; recipient_count: number; sent_count: number; failed_count: number; created_at: string; completed_at?: string; number_label?: string };
export type CampaignStatus = 'scheduled'|'queued'|'sending'|'paused'|'completed'|'cancelled'|'failed';
export type Campaign = { id: string; name: string; message_template: string; status: CampaignStatus; scheduled_for?: string|null; recipient_count: number; queued_count: number; sent_count: number; failed_count: number; suppressed_count: number; created_at: string; started_at?: string|null; completed_at?: string|null; number_label?: string; media_url?:string|null; media_type?:'image'|'video'|'audio'|'document'|null; media_mime_type?:string|null; media_file_name?:string|null };
export type CampaignRecipient = { id: string; phone: string; name: string; status: 'queued'|'processing'|'sent'|'failed'|'suppressed'|'cancelled'; error_code?: string|null; sent_at?: string|null };
export type ContactList = { id: string; name: string; description: string; created_at: string; contact_count: number; contacts?: Array<{ phone: string; name: string; variables?: Record<string, string|number|boolean> }> };
export type MessageTemplate = { id: string; name: string; body: string; created_at: string; updated_at: string };
export type Suppression = { phone: string; reason: string; created_at: string };
export type InboxChat = { id: string; name: string; preview: string; timestamp?: number; unread: number; unread_count?: number; group: boolean; status?: 'open'|'pending'|'resolved'; assigned_to?: string|null };
export type InboxMessage = { id: string; text: string; timestamp: number; fromMe: boolean; status?: string; mediaType?:'image'|'video'|'audio'|'document'; mediaUrl?:string; fileName?:string };
export type ConversationNote = { id: string; body: string; author_id: string; created_at: string };
export type DashboardData = { user: { email: string }; workspaces: Workspace[]; numbers: WhatsAppNumber[]; members: Member[]; error?: string };
